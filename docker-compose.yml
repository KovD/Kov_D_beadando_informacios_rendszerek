version: '3.8'

services:
  frontend:
    build: ./Minimal_frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./REST_API_3_KovD
    ports:
      - "8080:8080"
    env_file:
      - .env